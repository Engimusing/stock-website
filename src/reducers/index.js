// import {} from '../actions'

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        case SOME_ACTION:
            
            break;
    
        default:
            break;
    }
}